#include <stdio.h>
#include "asm.h"
char vec[10];
char vec2[10];
char vec3[];
char *ptr1=vec;
char *ptr2=vec2;
char *ptr3=vec3;

int main(){
scanf("%s",vec);
scanf("%s",vec2);

str_cat();
printf("%s\n",vec3);

}